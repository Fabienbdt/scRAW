# Convenience dependencies for CPU, macOS, Linux, and platform-native
# accelerators. These ranges support ordinary development and installation;
# they are not the reference reproduction lock. Use requirements.txt when
# reproducing the validated reference results.

numpy>=1.26,<3
scipy>=1.11,<2
pandas>=2.1,<3
scikit-learn>=1.4,<2
matplotlib>=3.8,<4

anndata>=0.10,<1
scanpy>=1.10,<2
h5py>=3.10,<4

umap-learn>=0.5.5,<1
pynndescent>=0.5,<1
numba>=0.59,<1

hdbscan>=0.8.33,<1
igraph>=0.11,<2
leidenalg>=0.10,<1

# PyPI resolves the wheel appropriate for the current operating system.
torch>=2.2,<3
